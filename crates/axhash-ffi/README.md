# axhash-ffi

Lapisan FFI C-stable untuk `axhash-core`.

- Mengekspor simbol `extern "C"` yang stabil
- Menghasilkan `staticlib` dan `cdylib`
- Menggunakan `cbindgen` untuk `include/axhash.h`
- Cocok sebagai dasar binding C++, Python, Go, Zig, Swift, dan Kotlin
